`/{{handle}} <characters> <TRX amount>`

Place an automatically repeating bet. The bet will repeat every {{interval}} seconds. You need to provide the characters to bet on and the amount of TRX to use per bet.  

Example:
`/{{handle}} ab1f 50`